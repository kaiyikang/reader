给我一个简洁优雅的bash，或python uv run脚本，哪一个最简洁，最是业界流行的，就要哪个：

调用时，用户需要输入epub文件的路径

判断是否是epub，或者空输入，提示错误并退出

获取epub的总章节数，如果不带章节，则显示总页数

提示用户输入想要获取的子章节数，字页数

- 如果是一个数字，则从开头到这个数字
- 如果输入两个数字，则两个数字中间应使用“-”隔空
- 输入可以带空格，程序自动消除
- 除此以外，非法，并给予提示

输出截取后的epub，位置和输入epub一致，文件名是，源文件名+ 截取的范围
